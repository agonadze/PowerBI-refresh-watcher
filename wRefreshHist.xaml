<Window x:Class="pbiWatcher.wRefreshHist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pbiWatcher"
        mc:Ignorable="d"
        Title="Refresh History" Height="450" Width="800">
    <StackPanel Orientation="Vertical">
        <TextBlock x:Name="lblTitle" Text="Refresh History" FontSize="14" Margin="10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="left">
            <TextBlock Text="Timezone:" VerticalAlignment="Center" Margin="20,10,10,0"/>
            <RadioButton x:Name="rbTZlocal" Content="Local" GroupName="Timezone" IsChecked="True" Checked="rbTZlocal_Checked" Margin="0,10,10,0"/>
            <RadioButton x:Name="rbTZUtc" Content="UTC" GroupName="Timezone" Checked="rbTZUtc_Checked"  Margin="0,10,10,0"/>
        </StackPanel>
        <DataGrid x:Name="dgHist" Margin="5,20,5,0" SelectionUnit="FullRow" AutoGenerateColumns="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
            SelectionMode="Single"
            CanUserResizeColumns="True"
            CanUserSortColumns="False"   
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeRows="False"
            CanUserReorderColumns="False"
            SelectedItem="{Binding DObject, Mode=OneTime}"
            GridLinesVisibility="Horizontal"
            ScrollViewer.CanContentScroll="True"
            Visibility ="Visible" Loaded="dgHist_Loaded">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime}" Width="170"/>
                <DataGridTextColumn Header="End Time" Binding="{Binding EndTime}" Width="170"/>
                <DataGridTextColumn Header="Duration (mins)" Binding="{Binding Duration}" Width="100"/>
                <DataGridTextColumn Header="Refresh Type" Binding="{Binding RefreshType}" Width="100"/>
                <DataGridTextColumn Header="Failure Reason" Binding="{Binding ErrorMessage}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
    
</Window>
